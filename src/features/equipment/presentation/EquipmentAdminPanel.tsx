import { useEffect, useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { AlertCircle, CheckCircle, Edit2, Plus, Trash2, Wrench } from "lucide-react";
import { useAuth } from "@/lib/auth";
import { queryKeys } from "@/infrastructure/query/query-keys";
import type { EquipmentDraft, EquipmentDraftBrand, EquipmentDraftModel, EquipmentTypeRow, TechnicalField } from "../domain/equipment";
import { loadEquipmentCatalog, saveEquipmentHierarchy, saveTechnicalField } from "../infrastructure/equipment.repository";
import { AdminButton, AdminCard, AdminCardHeader, AdminIconButton, AdminPage, BtnPrimary, BtnSecondary, InternalBackButton, PageHeader, Section } from "@/shared/ui/admin/AdminLayout";
import { EmptyState, LoadingState, StatusBadge, Toast } from "@/shared/ui/admin/AdminFeedback";
import { FInput, FSelect, FToggle } from "@/shared/ui/admin/AdminFormControls";
import { PaginationBar } from "@/shared/ui/admin/AdminPagination";
import { Checkbox } from "@/shared/ui/primitives/checkbox";
import { slugify } from "@/shared/domain/formatters";

export function EquipmentAdminPanel({ onBack }: { onBack: () => void }) {
  const { hasPermission } = useAuth();
  const canView = hasPermission("equipment.view");
  const canViewTable = hasPermission("equipment.table.view");
  const canViewDetails = hasPermission("equipment.details.view");
  const canCreate = hasPermission("equipment.create");
  const canEdit = hasPermission("equipment.edit");
  const canDelete = hasPermission("equipment.delete");
  const canViewFields = hasPermission("equipment.technical_fields.view");
  const canManageFields = hasPermission("equipment.technical_fields.manage");
  const queryClient = useQueryClient();
  const catalogQuery = useQuery({ queryKey: queryKeys.equipment.catalog(), queryFn: loadEquipmentCatalog, enabled: canView && (canViewTable || canViewDetails || canCreate || canEdit || canViewFields || canManageFields) });
  const types = catalogQuery.data?.types ?? []; const brands = catalogQuery.data?.brands ?? []; const models = catalogQuery.data?.models ?? []; const technicalFields = catalogQuery.data?.technicalFields ?? [];
  const [drafts, setDrafts] = useState<EquipmentDraft[]>([]); const [formOpen, setFormOpen] = useState(false); const [activeArea, setActiveArea] = useState<"registered" | "fields">("registered");
  const [fieldFormOpen, setFieldFormOpen] = useState(false); const [editingField, setEditingField] = useState<TechnicalField | null>(null); const [fieldForm, setFieldForm] = useState({ label: "", field_key: "", field_type: "text" as "text" | "number", is_active: true });
  const loading = catalogQuery.isPending; const [saving, setSaving] = useState(false); const [toast, setToast] = useState<{ msg: string; type: "success" | "error" } | null>(null);
  const [typePage, setTypePage] = useState(1); const [typePageSize, setTypePageSize] = useState(5); const [fieldPage, setFieldPage] = useState(1); const [fieldPageSize, setFieldPageSize] = useState(5);
  useEffect(() => { if (catalogQuery.error) setToast({ msg: `Erro ao carregar equipamentos: ${catalogQuery.error instanceof Error ? catalogQuery.error.message : String(catalogQuery.error)}`, type: "error" }); }, [catalogQuery.error]);
  useEffect(() => { if (activeArea === "fields" && !canViewFields) setActiveArea("registered"); }, [activeArea, canViewFields]);
  const typeTotalPages = Math.max(1, Math.ceil(types.length / typePageSize)); const safeTypePage = Math.min(typePage, typeTotalPages); const pagedTypes = types.slice((safeTypePage - 1) * typePageSize, safeTypePage * typePageSize);
  const fieldTotalPages = Math.max(1, Math.ceil(technicalFields.length / fieldPageSize)); const safeFieldPage = Math.min(fieldPage, fieldTotalPages); const pagedFields = technicalFields.slice((safeFieldPage - 1) * fieldPageSize, safeFieldPage * fieldPageSize);
  useEffect(() => { if (typePage > typeTotalPages) setTypePage(typeTotalPages); }, [typePage, typeTotalPages]); useEffect(() => { if (fieldPage > fieldTotalPages) setFieldPage(fieldTotalPages); }, [fieldPage, fieldTotalPages]);
  const refresh = () => queryClient.invalidateQueries({ queryKey: queryKeys.equipment.all });
  const makeDraft = (type?: EquipmentTypeRow): EquipmentDraft => ({ id: type?.id, name: type?.name || "", is_active: type?.is_active ?? true, technicalFields: (catalogQuery.data?.technicalFieldLinks ?? []).filter(link => link.equipment_type_id === type?.id).map(link => ({ technical_field_id: link.technical_field_id, required: link.required, sort_order: link.sort_order })), brands: brands.filter(brand => brand.equipment_type_id === type?.id).map(brand => ({ id: brand.id, name: brand.name, is_active: brand.is_active, models: models.filter(model => model.equipment_brand_id === brand.id).map(model => ({ id: model.id, name: model.name, is_active: model.is_active })) })) });
  const openNew = () => { if (!canCreate) return; setDrafts([makeDraft()]); setFormOpen(true); }; const openEdit = (type: EquipmentTypeRow) => { if (!(canViewDetails && canEdit)) return; setDrafts([makeDraft(type)]); setFormOpen(true); };
  const addEquipment = () => canCreate && setDrafts(current => [...current, makeDraft()]); const updateDraft = (index: number, value: Partial<EquipmentDraft>) => setDrafts(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, ...value } : item));
  const addBrand = (ti: number) => canCreate && setDrafts(current => current.map((type, i) => i === ti ? { ...type, brands: [...type.brands, { name: "", is_active: true, models: [{ name: "", is_active: true }] }] } : type));
  const updateBrand = (ti: number, bi: number, value: Partial<EquipmentDraftBrand>) => setDrafts(current => current.map((type, i) => i === ti ? { ...type, brands: type.brands.map((brand, j) => j === bi ? { ...brand, ...value } : brand) } : type));
  const removeBrand = (ti: number, bi: number) => canDelete && setDrafts(current => current.map((type, i) => i === ti ? { ...type, brands: type.brands.filter((_, j) => j !== bi) } : type));
  const addModel = (ti: number, bi: number) => canCreate && setDrafts(current => current.map((type, i) => i === ti ? { ...type, brands: type.brands.map((brand, j) => j === bi ? { ...brand, models: [...brand.models, { name: "", is_active: true }] } : brand) } : type));
  const updateModel = (ti: number, bi: number, mi: number, value: Partial<EquipmentDraftModel>) => setDrafts(current => current.map((type, i) => i === ti ? { ...type, brands: type.brands.map((brand, j) => j === bi ? { ...brand, models: brand.models.map((model, k) => k === mi ? { ...model, ...value } : model) } : brand) } : type));
  const removeModel = (ti: number, bi: number, mi: number) => canDelete && setDrafts(current => current.map((type, i) => i === ti ? { ...type, brands: type.brands.map((brand, j) => j === bi ? { ...brand, models: brand.models.filter((_, k) => k !== mi) } : brand) } : type));
  const updateTechnicalField = (ti: number, fieldId: string, checked: boolean) => { if (!canManageFields) return; setDrafts(current => current.map((type, i) => { if (i !== ti) return type; const existing = type.technicalFields.find(field => field.technical_field_id === fieldId); return { ...type, technicalFields: checked ? existing ? type.technicalFields : [...type.technicalFields, { technical_field_id: fieldId, required: false, sort_order: type.technicalFields.length * 10 }] : type.technicalFields.filter(field => field.technical_field_id !== fieldId) }; })); };
  const updateTechnicalFieldRequired = (ti: number, fieldId: string, required: boolean) => { if (!canManageFields) return; setDrafts(current => current.map((type, i) => i === ti ? { ...type, technicalFields: type.technicalFields.map(field => field.technical_field_id === fieldId ? { ...field, required } : field) } : type)); };
  const openNewField = () => { if (!canManageFields) return; setEditingField(null); setFieldForm({ label: "", field_key: "", field_type: "text", is_active: true }); setFieldFormOpen(true); }; const openEditField = (field: TechnicalField) => { if (!canManageFields) return; setEditingField(field); setFieldForm({ label: field.label, field_key: field.field_key, field_type: field.field_type, is_active: field.is_active }); setFieldFormOpen(true); };
  const saveField = async () => { if (!canManageFields) return; if (!fieldForm.label.trim() || (!editingField && !fieldForm.field_key)) { setToast({ msg: "Informe o nome do campo.", type: "error" }); return; } setSaving(true); try { await saveTechnicalField({ id: editingField?.id, label: fieldForm.label, field_key: editingField?.field_key || slugify(fieldForm.label).replace(/-/g, "_"), field_type: fieldForm.field_type, is_active: fieldForm.is_active, sort_order: editingField?.sort_order ?? technicalFields.length * 10 }); setFieldFormOpen(false); setToast({ msg: editingField ? "Campo atualizado." : "Campo cadastrado." }); await refresh(); } catch (error) { setToast({ msg: `Erro ao salvar campo: ${error instanceof Error ? error.message : String(error)}`, type: "error" }); } finally { setSaving(false); } };
  const toggleField = async (field: TechnicalField) => { if (!canManageFields) return; try { await saveTechnicalField({ ...field, label: field.label, field_key: field.field_key, sort_order: field.sort_order, is_active: !field.is_active }); await refresh(); } catch (error) { setToast({ msg: `Erro ao atualizar campo: ${error instanceof Error ? error.message : String(error)}`, type: "error" }); } };
  const save = async () => { if (!drafts.every(type => type.id ? canEdit : canCreate)) return; if (drafts.some(type => !type.name.trim() || type.brands.some(brand => !brand.name.trim() || brand.models.some(model => !model.name.trim())))) { setToast({ msg: "Preencha equipamento, marcas e modelos antes de salvar.", type: "error" }); return; } setSaving(true); try { await saveEquipmentHierarchy(drafts, { types, brands, models }); setFormOpen(false); setToast({ msg: "Equipamentos salvos com sucesso.", type: "success" }); await refresh(); } catch (error) { setToast({ msg: `Erro ao salvar estrutura: ${error instanceof Error ? error.message : String(error)}`, type: "error" }); } finally { setSaving(false); } };

  if (!canView) return null;

  return <div className="min-w-0 space-y-5">
    {toast && <Toast message={toast.msg} type={toast.type} onClose={() => setToast(null)} />}
    <PageHeader title="Equipamentos Técnicos" subtitle="Cadastro hierárquico usado nas ordens de serviço" actions={<div className="flex items-center gap-2"><InternalBackButton onBack={onBack} />{canCreate && <AdminButton onClick={openNew} className="text-xs"><Plus size={15} /> Novo equipamento</AdminButton>}</div>} />
    <div className="flex gap-1 border-b border-[#0d1b2e]/10">{canViewTable && <button type="button" onClick={() => setActiveArea("registered")} className={`cursor-default border-b-2 px-4 py-2.5 text-xs font-bold ${activeArea === "registered" ? "border-[#0057e7] text-[#0057e7]" : "border-transparent text-[#5a6a82]"}`}>Cadastrados</button>}{canViewFields && <button type="button" onClick={() => setActiveArea("fields")} className={`cursor-default border-b-2 px-4 py-2.5 text-xs font-bold ${activeArea === "fields" ? "border-[#0057e7] text-[#0057e7]" : "border-transparent text-[#5a6a82]"}`}>Campos</button>}</div>
    {activeArea === "registered" && canViewTable ? <AdminCard>{loading ? <LoadingState /> : types.length === 0 ? <EmptyState icon={Wrench} title="Nenhum equipamento cadastrado" message="Cadastre o primeiro equipamento com suas marcas e modelos." onAdd={canCreate ? openNew : undefined} addLabel="Novo equipamento" /> : <><div className="overflow-x-auto"><table className="min-w-[680px]"><thead><tr><th className="text-left">Equipamento</th><th className="text-left">Marcas</th><th className="text-left">Modelos</th><th className="text-left">Status</th><th className="text-right">Ações</th></tr></thead><tbody>{pagedTypes.map(type => { const typeBrands = brands.filter(brand => brand.equipment_type_id === type.id); const brandIds = new Set(typeBrands.map(brand => brand.id)); const modelCount = models.filter(model => brandIds.has(model.equipment_brand_id)).length; return <tr key={type.id}><td className="font-bold text-[#0d1b2e]">{type.name}</td><td className="text-xs text-[#5a6a82]">{typeBrands.length}</td><td className="text-xs text-[#5a6a82]">{modelCount}</td><td><StatusBadge status={type.is_active ? "Ativo" : "Inativo"} /></td><td><div className="flex justify-end gap-1">{canViewDetails && canEdit && <AdminIconButton ariaLabel="Editar equipamento" title="Editar equipamento" onClick={() => openEdit(type)}><Edit2 size={14} /></AdminIconButton>}</div></td></tr>; })}</tbody></table></div><PaginationBar page={safeTypePage} pageSize={typePageSize} totalItems={types.length} onPageChange={setTypePage} onPageSizeChange={size => { setTypePageSize(size); setTypePage(1); }} /></>}</AdminCard> : activeArea === "fields" && canViewFields ? <AdminCard><AdminCardHeader className="justify-end">{canManageFields && <AdminButton onClick={openNewField} className="text-xs"><Plus size={15} /> Novo campo</AdminButton>}</AdminCardHeader>{loading ? <LoadingState /> : technicalFields.length === 0 ? <EmptyState icon={Wrench} title="Nenhum campo técnico cadastrado" message="Cadastre o primeiro campo técnico." onAdd={canManageFields ? openNewField : undefined} addLabel="Novo campo" /> : <><div className="overflow-x-auto"><table className="min-w-[680px]"><thead><tr><th className="text-left">Campo</th><th className="text-left">Chave</th><th className="text-left">Tipo</th><th className="text-left">Status</th><th className="text-right">Ações</th></tr></thead><tbody>{pagedFields.map(field => <tr key={field.id}><td className="font-bold text-[#0d1b2e]">{field.label}</td><td className="font-mono text-xs text-[#5a6a82]">{field.field_key}</td><td className="text-xs text-[#5a6a82]">{field.field_type === "number" ? "Número" : "Texto"}</td><td><StatusBadge status={field.is_active ? "Ativo" : "Inativo"} /></td><td><div className="flex justify-end gap-1">{canManageFields && <><AdminIconButton ariaLabel="Editar campo" title="Editar campo" onClick={() => openEditField(field)}><Edit2 size={14} /></AdminIconButton><AdminIconButton ariaLabel={field.is_active ? "Inativar campo" : "Ativar campo"} title={field.is_active ? "Inativar campo" : "Ativar campo"} onClick={() => void toggleField(field)}>{field.is_active ? <CheckCircle size={14} /> : <AlertCircle size={14} />}</AdminIconButton></>}</div></td></tr>)}</tbody></table></div><PaginationBar page={safeFieldPage} pageSize={fieldPageSize} totalItems={technicalFields.length} onPageChange={setFieldPage} onPageSizeChange={size => { setFieldPageSize(size); setFieldPage(1); }} /></>}</AdminCard> : null}
    <AdminPage open={formOpen} onClose={() => setFormOpen(false)} breadcrumb="Equipamentos Técnicos" title="Cadastro de equipamentos" subtitle="Monte equipamento, marcas e modelos antes de salvar"><div className="space-y-5 p-4 sm:p-5">{drafts.map((type, ti) => <div key={`${type.id || "new"}-${ti}`} className="space-y-4"><Section title="Equipamento"><div className="flex items-end gap-2"><div className="min-w-0 flex-1"><FInput label="Nome do equipamento" required value={type.name} onChange={(e: any) => updateDraft(ti, { name: e.target.value })} placeholder="Ex: Televisão" /></div>{drafts.length > 1 && canDelete && <button type="button" onClick={() => setDrafts(current => current.filter((_, i) => i !== ti))} className="cursor-default rounded-lg p-2.5 text-red-500 hover:bg-red-50"><Trash2 size={16} /></button>}</div></Section>{canViewFields && <Section title="Campos do equipamento"><div className="grid gap-3 sm:grid-cols-2">{technicalFields.filter(field => field.is_active || type.technicalFields.some(link => link.technical_field_id === field.id)).map(field => { const link = type.technicalFields.find(item => item.technical_field_id === field.id); return <AdminCard key={field.id} className="px-3 py-3 shadow-none"><label className="flex cursor-default items-start gap-2 text-sm text-[#0d1b2e]"><Checkbox disabled={!canManageFields} checked={Boolean(link)} onCheckedChange={checked => updateTechnicalField(ti, field.id, checked === true)} /><span className="min-w-0 flex-1 break-words font-semibold">{field.label}</span></label>{link && <label className="mt-2 flex cursor-default items-center gap-2 pl-6 text-xs text-[#5a6a82]"><Checkbox disabled={!canManageFields} checked={link.required} onCheckedChange={checked => updateTechnicalFieldRequired(ti, field.id, checked === true)} />Obrigatório</label>}</AdminCard>; })}</div></Section>}<Section title="Marcas e modelos"><div className="flex items-center justify-end">{canCreate && <AdminButton onClick={() => addBrand(ti)} className="text-xs"><Plus size={14} /> Nova marca</AdminButton>}</div><div className="mt-3 space-y-3">{type.brands.map((brand, bi) => <AdminCard key={`${brand.id || "new-brand"}-${bi}`} className="bg-[#f8fafc] p-4 shadow-none"><div className="flex items-end gap-2"><div className="min-w-0 flex-1"><FInput label="Marca" required value={brand.name} onChange={(e: any) => updateBrand(ti, bi, { name: e.target.value })} /></div>{canDelete && <button type="button" onClick={() => removeBrand(ti, bi)} className="cursor-default rounded-lg p-2.5 text-red-500"><Trash2 size={15} /></button>}</div><div className="mt-3 space-y-2 border-l-2 border-[#0057e7]/20 pl-3"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase text-[#5a6a82]">Modelos</span>{canCreate && <AdminButton onClick={() => addModel(ti, bi)} className="text-xs"><Plus size={14} /> Novo modelo</AdminButton>}</div>{brand.models.map((model, mi) => <div key={`${model.id || "new-model"}-${mi}`} className="flex items-center gap-2"><div className="min-w-0 flex-1"><FInput value={model.name} required onChange={(e: any) => updateModel(ti, bi, mi, { name: e.target.value })} /></div>{canDelete && <button type="button" onClick={() => removeModel(ti, bi, mi)} className="cursor-default rounded-lg p-2 text-red-500"><Trash2 size={14} /></button>}</div>)}</div></AdminCard>)}</div></Section></div>)}{canCreate && drafts.every(type => !type.id) && <BtnSecondary onClick={addEquipment}><Plus size={15} /> Adicionar equipamento</BtnSecondary>}</div><div className="sticky bottom-0 flex justify-end gap-3 border-t border-[#0d1b2e]/8 bg-white px-4 py-4 sm:px-5"><BtnSecondary onClick={() => setFormOpen(false)}>Cancelar</BtnSecondary><BtnPrimary onClick={save} disabled={saving}>{saving ? "Salvando..." : "Salvar estrutura"}</BtnPrimary></div></AdminPage>
    <AdminPage open={fieldFormOpen && canManageFields} onClose={() => setFieldFormOpen(false)} breadcrumb="Equipamentos Técnicos > Campos" title={editingField ? "Editar campo técnico" : "Novo campo técnico"} subtitle="Configure um campo reutilizável nos tipos de equipamento"><div className="space-y-5 p-4 sm:p-5"><Section title="Campo técnico"><div className="grid gap-4"><FInput label="Nome do campo" required value={fieldForm.label} onChange={(e: any) => setFieldForm(current => ({ ...current, label: e.target.value, ...(!editingField && !current.field_key ? { field_key: slugify(e.target.value).replace(/-/g, "_") } : {}) }))} /><FInput label="Chave" required disabled={Boolean(editingField)} value={fieldForm.field_key} onChange={(e: any) => setFieldForm(current => ({ ...current, field_key: e.target.value }))} /><FSelect label="Tipo" value={fieldForm.field_type} onChange={(e: any) => setFieldForm(current => ({ ...current, field_type: e.target.value }))} options={[{ value: "text", label: "Texto" }, { value: "number", label: "Número" }]} /><FToggle label="Campo ativo" checked={fieldForm.is_active} onChange={is_active => setFieldForm(current => ({ ...current, is_active }))} /></div></Section></div><div className="sticky bottom-0 flex justify-end gap-3 border-t border-[#0d1b2e]/8 bg-white px-4 py-4 sm:px-5"><BtnSecondary onClick={() => setFieldFormOpen(false)}>Cancelar</BtnSecondary><BtnPrimary onClick={() => void saveField()} disabled={saving}>{saving ? "Salvando..." : "Salvar"}</BtnPrimary></div></AdminPage>
  </div>;
}
